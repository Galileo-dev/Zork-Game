/*
View is a class that represents a view that the player can see.
*/
enum VIEW {
    VIEW_PICKUP_ITEM,
    VIEW_DROP_ITEM,
    VIEW_USE_ITEM,
}

/*
Action is a class that represents an action that the player can take.
*/
enum ACTION {
    ACTION_PICKUP_ITEM,
    ACTION_DROP_ITEM,
    ACTION_USE_ITEM,
}